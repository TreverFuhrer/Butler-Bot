package com.toki.clever.DirectMessaging.Modules;

public class DailyBible {

    // Listener to check if user updates bible read amount
    // Then Call Bible to get 1 hour of verses to read

    // 1. Saves bible read amount to json
    // 2. Can be interacted with in DM
    // 3. Makes Gettable data about the bible for the day

    // Create enum for [day, amt to read]
}
